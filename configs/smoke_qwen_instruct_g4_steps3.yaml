# Smoke test config with longer output budget and instruct model
env:
  group_size: 4
  batch_size: 4
  max_batches_offpolicy: 1
  tokenizer_name: "Qwen/Qwen3-4B-Instruct-2507"
  use_wandb: false
  rollout_server_url: "http://localhost:8000"
  wandb_name: "atropos-tinker-smoke-instruct-g4-env"
  ensure_scores_are_not_same: true
  max_token_length: 384
  max_num_workers: 8
  total_steps: 3
  steps_per_eval: 100

openai:
  - model_name: "Qwen/Qwen3-4B-Instruct-2507"
    base_url: "http://localhost:8001/v1"
    api_key: "x"
    weight: 1.0
    num_requests_for_eval: 8
    server_type: "sglang"

# Tinker-specific configuration
tinker:
  lora_rank: 8
  learning_rate: 0.00004
  max_token_trainer_length: 768
  checkpoint_dir: "./temp/"
  save_checkpoint_interval: 0
  wandb_project: "atropos-tinker-smoke-instruct-g4"
  wandb_group: null
  wandb_run_name: "atropos-tinker-smoke-instruct-g4-run"

slurm: false
testing: false
